# Static Blog Site

This directory contains a dependency-free static personal blog scaffold.

Open `index.html` directly in a browser to preview it. No dev server is required for the current version.
