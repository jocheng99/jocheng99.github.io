# Config

Store blog configuration examples and local path templates here.

- Commit example files such as `blog.config.example.json`.
- Keep private tokens, analytics secrets, deployment credentials, and machine-local secrets out of this project.
- If a local-only config is needed later, name it with `.local` and do not treat it as publishable content.
