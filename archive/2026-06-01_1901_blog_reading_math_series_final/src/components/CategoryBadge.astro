---
import { taxonomyPath } from "../utils/taxonomy";
import { withBase } from "../utils/paths";

interface Props {
  category: string;
}

const { category } = Astro.props;
---

<a class="category-badge" href={withBase(taxonomyPath("categories", category))}>
  {category}
</a>
